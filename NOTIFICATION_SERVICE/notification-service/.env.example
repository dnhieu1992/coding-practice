# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=notification
DB_PASS=notification_password
DB_NAME=notification_db

# Optional: Use socket path instead of host/port (for local MySQL installations)
# DB_SOCKET_PATH=/tmp/mysql.sock

# Application Configuration
PORT=3000
NODE_ENV=development
